<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:controls="using:DatasetStudio.Controls"
             x:Class="DatasetStudio.Controls.TagPill"
             x:Name="Root"
             x:DataType="controls:TagPill">
  <Border Classes="surface-card pad-compact">
    <Grid ColumnDefinitions="Auto,Auto" VerticalAlignment="Center">
      <TextBlock Text="{Binding #Root.Tag}"
                 Classes="mono"
                 VerticalAlignment="Center" />
      <Button Grid.Column="1"
              Command="{Binding #Root.RemoveCommand}"
              CommandParameter="{Binding #Root.Tag}"
              Classes="chromeless"
              Margin="8,0,0,0"
              VerticalAlignment="Center">
        <TextBlock Text="x"
                   Classes="mono subtle-text"
                   FontWeight="SemiBold"
                   />
      </Button>
    </Grid>
  </Border>
</UserControl>
