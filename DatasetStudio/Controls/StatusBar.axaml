<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:controls="using:DatasetStudio.Controls"
             x:Class="DatasetStudio.Controls.StatusBar"
             x:Name="Root"
             x:DataType="controls:StatusBar">
  <Border Classes="chrome-shell panel">
    <Grid ColumnDefinitions="Auto,*"
          Classes="info-bar-layout">
      <Border Classes="label-badge status">
        <TextBlock Text="STATUS"
                   Classes="mono label-badge-text status" />
      </Border>
      <TextBlock Grid.Column="1"
                 Text="{Binding #Root.Text}"
                 Classes="shell-bar-text"
                 TextTrimming="CharacterEllipsis" />
    </Grid>
  </Border>
</UserControl>
